const q=document.querySelector('#q');const buttons=[...document.querySelectorAll('.filter')];const cards=[...document.querySelectorAll('[data-tags]')];let active='all';function apply(){const needle=(q?.value||'').trim().toLowerCase();let shown=0;cards.forEach(c=>{const tags=(c.dataset.tags||'').toLowerCase();const text=c.innerText.toLowerCase();const okTag=active==='all'||tags.includes(active);const okText=!needle||text.includes(needle);const visible=okTag&&okText;c.classList.toggle('hidden',!visible);if(visible)shown++;});const count=document.querySelector('#result-count');if(count)count.textContent=shown+' item'+(shown===1?'':'s')+' shown';}q?.addEventListener('input',apply);buttons.forEach(b=>b.addEventListener('click',()=>{active=b.dataset.filter;buttons.forEach(x=>x.classList.toggle('active',x===b));apply();}));apply();