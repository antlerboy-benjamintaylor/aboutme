const q=document.querySelector('#q');const buttons=[...document.querySelectorAll('.filter')];const cards=[...document.querySelectorAll('.card[data-tags]')];let active='all';function apply(){const needle=(q?.value||'').trim().toLowerCase();cards.forEach(c=>{const tags=c.dataset.tags||'';const text=c.innerText.toLowerCase();const okTag=active==='all'||tags.includes(active);const okText=!needle||text.includes(needle);c.classList.toggle('hidden',!(okTag&&okText));});}q?.addEventListener('input',apply);buttons.forEach(b=>b.addEventListener('click',()=>{active=b.dataset.filter;buttons.forEach(x=>x.classList.toggle('active',x===b));apply();}));